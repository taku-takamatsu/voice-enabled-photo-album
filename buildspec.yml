version: 0.1
phases:
  install:
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket ccdb-lambda-pipeline --output-template-file outputsamtemplate.yml --capabilities CAPABILITY_IAM
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputsamtemplate.yml